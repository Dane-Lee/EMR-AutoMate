# ── Sensitive — NEVER commit (login session / employee PHI) ──
# Live EMR browser session: login cookies/credentials
.browser_profile/
# Screenshots + page HTML of the live EMR (employee PHI)
debug/
# Working input with real employee encounter data (PHI)
encounters.csv
# Parked PA Follow-Up assessments (PHI) — tool can't draft these yet
pa_follow_ups.csv
# Working input with the real employee roster (PII) — template is safe to commit
roster.xlsx
# Audit log of employee updates (old->new employee PII)
employee_updates_log.csv
# Generated reports with employee PII
gender_review_needed.csv
nickname_candidates.csv
identifier_shortened.csv
emr_not_in_roster.csv
# Roster rows not found in the EMR (new-hire worklist, PII) + targeted re-run sheet
roster_not_in_emr.csv
roster_rerun.xlsx
# Identifier-cleanup fix sheet (truncated IDs to re-push, PII)
roster_idfix.xlsx
# Blank Date-of-Hire worklist + DoH-only fix/test sheets + failure report (PII)
date_of_hire_todo.csv
date_not_persisted.csv
roster_dohfix.xlsx
roster_dohtest.xlsx
# Running log of newly-composed encounter descriptions (to merge into the worksheet)
new_descriptions_for_library.csv
# Text dump of the description workbook, so Copilot can read it (library_export.py).
# Ignored for the same reason its source workbook is.
description_library.md
# Paste-ready M365 Copilot prompt = copilot_spec.md + the library (make_copilot_prompt.py).
# The SPEC is tracked; this generated merge is not, because it embeds the library.
copilot_prompt.md
# Assessments Copilot parked for Dane to enter by hand (PHI)
assessments_todo.md
# Descriptions Copilot had to invent, to fold back into the workbook (PHI)
new_descriptions_todo.md
# Previous encounters.csv, kept by Paste-Encounters.ps1 so a batch is never destroyed
encounters.bak.csv
# Per-run encounter audit log (real employee names + outcomes, PII/PHI). Review it
# redacted with:  python ati_coaching_encounter.py --audit
encounter_log.csv
# Real employee data / notes dropped into the project folder (PII / PHI)
*EMR_notes*
Active Associates*.xlsx
EMR Easy Enter Worksheets.xlsx
# Stray / scratch files
Untitled-*.txt

# ── Python ──
__pycache__/
*.pyc
*.pyo
*.pyd

# ── OS / editor cruft ──
Thumbs.db
.DS_Store
*.swp
